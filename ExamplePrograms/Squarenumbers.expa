list<[int]> SquareNumbers(immut list<[int]> Input)
{
    list<[int]> OutputList = new list<[int]>(Size: GetSize(Input));
    for(int i = 0; i < GetSize(Input); i++)
    {
        OutputList[i] = Input[i] ** 2;
    }
}