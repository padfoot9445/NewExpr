list<[int]> SquareNumbers(immut list<[int]> _Input)
{
    list<[int]> OutputList = new list<[int]>(Size: GetLength(_Input));
    for(int i = 0; i < GetLength(_Input); ++i)
    {
        OutputList[i] = _Input[i] ** 2;
    }
}